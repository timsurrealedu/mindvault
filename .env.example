# MindVault environment configuration
# Copy to .env and adjust for your setup.

# Prisma database connection (SQLite for local dev; swap to Postgres in prod)
DATABASE_URL="file:./dev.db"

# API server port (Vite proxies /api -> this)
PORT=4000
